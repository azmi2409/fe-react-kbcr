import { Navbar } from "@mantine/core";

const SideBar = () => {
  return (
    <Navbar
      width={{
        base: 300,
      }}
      height="100vh"
      p="xs"
      className="transition-all delay-200 bg-gray-100"
    >
      test
    </Navbar>
  );
};

export default SideBar;
